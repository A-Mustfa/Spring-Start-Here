creating singleton instance with eager instantiation -> Spring is default approach

** we added constructor to the comment repo class that displays a text while initializing
-> the bean is created while loading the context