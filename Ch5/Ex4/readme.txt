creating singleton instance with Lazy instantiation -> @Lazy Annotation

** we added constructor to the comment repo class that displays a text while initializing
-> the bean is created when we refer to it for first time